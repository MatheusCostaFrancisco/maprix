# URL da API do backend. Em dev com Docker (docker compose up), use:
VITE_API_URL=http://localhost:3000

# Token público do Mapbox (mapa do polígono). Sem ele, o mapa mostra um fallback.
# Pegue em https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=
